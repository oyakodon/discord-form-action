{
  "name": "discord-form-action",
  "version": "0.1.0",
  "description": "Discord Bot for form input management using Cloudflare Workers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "check": "biome check .",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "discord-api-types": "^0.38.37",
    "discord-interactions": "^4.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@cloudflare/vitest-pool-workers": "^0.12.3",
    "@cloudflare/workers-types": "^4.20260114.0",
    "@types/bun": "latest",
    "discord.js": "^14.25.1",
    "typescript": "^5",
    "vitest": "^4.0.17",
    "wrangler": "^4.59.1"
  }
}
